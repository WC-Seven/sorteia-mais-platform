---
description: Uso de @ts-ignore e Tratamento de Warnings
globs:
alwaysApply: true
---

- **Nunca** utilize `@ts-ignore` sem um comentário explicando claramente o motivo da necessidade de ignorar o erro.
- Todo uso de `@ts-ignore` deve ser acompanhado de uma justificativa técnica imediatamente acima da diretiva.
- **Nunca** ignore erros de typescript sem documentação.
- Warnings de compilação ou lint devem ser tratados com a mesma seriedade que erros: **corrija-os antes de prosseguir**.

Exemplo correto:
```ts
// Necessário devido à limitação da biblioteca x, ver a issue #123
// @ts-ignore
algumaFuncao();
```
Exemplo incorreto:
```ts
// @ts-ignore
algumaFuncao();
```